---
title: "Caption Camera Follow"
description: "New words stay fixed on an expanding page while the camera pulls back exponentially until the complete sentence is readable."
---

import { PrimitivePlayer } from "/snippets/PrimitivePlayer.jsx";

<span className="hf-primitive-eyebrow">Camera moves</span>

<PrimitivePlayer name="caption-camera-follow" width={1920} height={1080} />

## When to use it

A sentence must accumulate word by word while each arrival reads at roughly the same screen size.

## When to avoid it

Avoid placing scripts outside the composition root, because the host copies that markup without its behavior and produces a black card with no timeline.

## Pairs with

- [kinetic-type-swap](/primitives/kinetic-type-swap)
- [glyph-ring-assemble](/primitives/glyph-ring-assemble)
