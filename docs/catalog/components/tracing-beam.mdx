---
title: "Tracing Beam"
description: "A glowing dash follows one SVG path through three UI elements, lifting and brightening each element in sequence."
---

import { InstallCommand } from "/snippets/install-command.jsx";

<iframe src="/public/catalog/preview/components/tracing-beam.html" className="w-full aspect-video rounded-xl border border-zinc-200 dark:border-zinc-800" loading="lazy" title="Tracing Beam preview" />

## Install

<InstallCommand command="npx hyperframes add tracing-beam" />

That writes one file: `compositions/components/tracing-beam.html`.

## Paste it into your composition

Open `compositions/components/tracing-beam.html` and copy what is inside into your own composition.

A component has no size or duration of its own. It takes both from the composition
you paste it into.

## Variables

Every one of these has a default, so the piece works untouched. Set the ones you
want to change on the element:

| Variable | Default | Accepts | What it does |
| --- | --- | --- | --- |
| `labels` | `Search,Preview,Export` | string | Three comma-separated labels shown in path order. |
| `accent` | `cyan` | `cyan`, `violet`, `amber` | Beam and active-element color. |

{/* hf:generated-footer */}

Tagged `motion-primitive` `product-demo` `attention` `beam` `path` `spotlight`.

## Related topics

- [Browse the complete Catalog](/catalog)
- [Add assets and Catalog items in Studio](/studio/assets-and-blocks)
- [Build a richer composition](/go-further)
