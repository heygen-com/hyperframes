# Studio open work-count fixture

A 60 s film of twelve 5 s scenes, each its own composition file, with no media. It has the
shape of a real multi-scene film (one thumbnail per scene, one preview document) at a size
that belongs in the repository. `studio-open-counts.mjs` opens it and counts the work Studio
does until the film can play and in the 20 s after.
